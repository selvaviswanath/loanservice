services:

  # Loan evaluation API — no external database required
  loan-service:
    build: .
    container_name: loan-service
    ports:
      - "8080:8080"
    volumes:
      # Persist the JSON database across container restarts
      - ./data:/app/data
